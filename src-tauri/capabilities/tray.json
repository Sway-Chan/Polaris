{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "tray",
  "description": "托盘自绘浮层窗（label=tray）能力集：订阅代理生命周期事件 + 读平台标识；自绘浮层的连接/断开/切节点/打开主窗/退出经自定义 command（app command 不受 ACL 限制）。窗口显隐/尺寸由 Rust 侧 tray 模块管理，故不授前端 window 写权限。",
  "windows": ["tray"],
  "permissions": ["core:default", "core:event:default", "os:default"]
}
